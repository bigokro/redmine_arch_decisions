<%= labelled_form_for @discussion, :url => { :action => "edit", :id => @discussion.id }, :html => {:multipart => true} do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save), :tabindex => autotab %>
<% end %>
