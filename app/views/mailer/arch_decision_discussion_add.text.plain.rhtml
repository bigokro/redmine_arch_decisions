<%= l(:text_arch_decision_discussion_added, @discussion.parent_type, "##{@discussion.parent.id}", @discussion.created_by) %>

----------------------------------------
<%= render :partial => "discussion_text_plain", :locals => { :discussion => @discussion, :discussion_url => @discussion_url, :parent_description => @parent_description } %>
