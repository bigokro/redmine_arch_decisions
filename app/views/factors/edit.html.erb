<%= labelled_form_for @factor, :url => { :project_id => @project.id, :action => "edit", :id => @factor.id } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save), :tabindex => autotab %>
<% end %>
