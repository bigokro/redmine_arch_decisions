<h2><%=l(:label_factor_new)%></h2>

<% labelled_tabular_form_for :factor, @factor, :url => { :project_id => @project, :action => "new" } do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>

<%= submit_tag l(:button_save), :tabindex => autotab %>
<% end %>
